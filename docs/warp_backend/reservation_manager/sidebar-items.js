window.SIDEBAR_ITEMS = {"fn":[["_check_for_unreserved_steps","Simple reservation logic that looks for steps with satisfied requirements and allocates resources through  reservation mechanism (slurm plugin). This method should be only for testing purposes as it bypasses optimization module"],["optimization_lookup","This function is the entry point for the Optimizer"],["reservation_loop","reservation_loop() starts a infinite loop that every $RESERVATION_LOOP_INTERVAL seconds perform a reservation cycle. This means that reservations are performed asynchronously to the submission of workflows "]]};